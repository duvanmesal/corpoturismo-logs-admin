# API principal (Render). El panel solo habla con esta API, nunca con el LogService.
VITE_API_URL=http://localhost:3000/api/v1

VITE_APP_NAME=Corpoturismo Logs Admin
VITE_DEFAULT_TZ=America/Bogota

# NO incluir aquí: READ_API_KEY, INGEST_API_KEY, secretos JWT, ni URLs de MongoDB.
# Esas claves viven SOLO en el servidor de la API principal.
